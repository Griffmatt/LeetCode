var countGoodSubstrings = function(s) {
    let count = 0
   for(i=0; i<s.length-2; i++){
       let set = new Set(s.slice(i, i+3))
       
       if(set.size === 3) count++
   } 
    
    return count
};
