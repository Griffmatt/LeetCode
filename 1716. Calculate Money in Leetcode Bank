var totalMoney = function(n) {
    let weeks = Math.floor(n/7)
    let remainder = n%7
    let total = 0
    for(i=0; i<weeks; i++){
       total += 28+(i*7) 
    }
    total += ((remainder*remainder)+remainder)/2+(weeks*remainder)
    
    return total
};
