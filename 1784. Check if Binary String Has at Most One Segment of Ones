var checkOnesSegment = function(s) {
    let count = 0
   for(i=0; i<s.length; i++){
           while(s[i] === "1"){
               i++
           }
           count++
       }
   } 

    return count === 1
};
