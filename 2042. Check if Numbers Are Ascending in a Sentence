var areNumbersAscending = function(s) {
    s = s.split(" ")
    let prev = 0
    for(i=0; i<s.length; i++){
        if(isNaN(s[i])) continue
      if(Number(s[i])<=Number(prev)) return false
        prev = s[i]
    }
  return true
};
