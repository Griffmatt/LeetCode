var areNumbersAscending = function(s) {
    s = s.split(" ").filter(number => !isNaN(number))
    for(i=0; i<s.length; i++){
      if(Number(s[i])>=Number(s[i+1])) return false
    }
  return true
};
